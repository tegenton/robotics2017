import radio
import random
from microbit import *

# The radio won't work unless it's switched on.
radio.on()

//define directional signals
forward = 
backward =
left =
right = 

while True:
  # if forward
    radio.send(forward)
  # if backward
    radio.send(backward)
  # if left
    radio.send(left)
  # if forward
    radio.send(right)
